<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="8dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Game Thumbnail -->
        <ImageView
            android:id="@+id/favGameThumbnail"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginEnd="12dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/game_thumbnail"
            android:background="@color/material_dynamic_surface_variant" />

        <!-- Game Info Section -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_gravity="center_vertical">

            <!-- Game Title -->
            <TextView
                android:id="@+id/favGameTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Game Title"
                android:textSize="16sp"
                android:textColor="@color/material_dynamic_on_surface"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginBottom="4dp" />

            <!-- Store Name -->
            <TextView
                android:id="@+id/favStoreName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Store"
                android:textSize="12sp"
                android:textColor="@color/material_dynamic_on_surface_variant"
                android:layout_marginBottom="8dp" />

            <!-- Price Section -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Sale Price -->
                <TextView
                    android:id="@+id/favSalePrice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="$14.99"
                    android:textSize="16sp"
                    android:textColor="@color/material_dynamic_primary"
                    android:textStyle="bold" />

                <!-- Discount Badge -->
                <LinearLayout
                    android:id="@+id/favDiscountBadge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="2dp"
                    android:background="@drawable/discount_badge_background"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/favDiscountPercentage"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="-50%"
                        android:textSize="12sp"
                        android:textColor="@color/white"
                        android:textStyle="bold" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>

        <!-- Remove Button -->
        <ImageButton
            android:id="@+id/removeFavBtn"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:layout_gravity="center_vertical"
            android:src="@drawable/ic_remove_favorite"
            android:contentDescription="@string/remove_favorite"
            android:scaleType="centerInside"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
